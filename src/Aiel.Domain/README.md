# Aiel.Domain

`Aiel.Domain` contains the domain-layer base contracts such as aggregates, entities, repositories, and domain events.

Typed identifier contracts are provided by `Aiel.StrongIds`; domain base types depend on that package for `IStrongId` constraints.