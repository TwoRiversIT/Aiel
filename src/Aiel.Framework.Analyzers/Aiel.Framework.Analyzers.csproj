<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <RootNamespace />
        <AssemblyName>Aiel.Framework.Analyzers</AssemblyName>
        <TargetFramework>netstandard2.0</TargetFramework>
        <IsPackable>true</IsPackable>

        <!--
        These are settings specific to analyzers/code fixes/source generators. IncludeBuildOutput is false
        to prevent the DLL landing in lib/, but the assembly is explicitly packed into analyzers/dotnet/cs.
        Analyzers can be deterministic because they only read code and do not embed timestamps.
        -->
        <Deterministic>true</Deterministic>
        <IncludeBuildOutput>false</IncludeBuildOutput>
        <IsRoslynComponent>true</IsRoslynComponent>
        <DevelopmentDependency>true</DevelopmentDependency>
        <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
        <NoWarn>$(NoWarn);NU5128;RS2000</NoWarn>
    </PropertyGroup>

    <ItemGroup>
        <None Include="./README.md" Pack="true" PackagePath="/" />
    </ItemGroup>

    <!-- BEGIN: Generator and Analyzer specific settings -->
    <ItemGroup>
        <AdditionalFiles Include="AnalyzerReleases.Shipped.md" />
        <AdditionalFiles Include="AnalyzerReleases.Unshipped.md" />
    </ItemGroup>

    <ItemGroup>
        <Compile Remove="bin\**" />
        <EmbeddedResource Remove="bin\**" />
        <None Remove="bin\**" />
        <None Remove="bin\Debug\netstandard2.0\" />
        <None Remove="bin\Debug\netstandard2.0\$(AssemblyName).dll" />
    </ItemGroup>

    <ItemGroup>
        <None Include="$(OutputPath)$(AssemblyName).dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
        <Compile Include="..\Aiel.SharedCode\Aiel\CodeAnalysis\DiagnosticMetadata.cs" Pack="false" Path="\Aiel\CodeAnalysis\DiagnosticMetadata.cs" />
        <Compile Include="..\Aiel.SharedCode\Aiel\CodeAnalysis\DiagnosticRuleIDs.cs" Pack="false" Path="\Aiel\CodeAnalysis\DiagnosticRuleIDs.cs" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="5.6.0" PrivateAssets="all" />
        <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="5.6.0">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers</IncludeAssets>
        </PackageReference>
    </ItemGroup>
    <!-- END: Generator and Analyzer specific settings -->

</Project>
